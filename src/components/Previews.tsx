import React, { useContext, useState, useEffect } from "react";
import * as Bl from "react-bulma-components";

const PreviewButton = () => {
  const preview = () => {
    
  };
  return (
    <Bl.Button onClick={preview}></Bl.Button>
  );
};